<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.easyquizy_app">

    <application
        android:name=".Model.App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/DarkyTheme">
        <activity
            android:name=".TopicStartActivity"
            android:label="@string/title_activity_topic_start"
            android:theme="@style/DarkyTheme.NoActionBar"></activity>
        <activity android:name=".Playing" />
        <activity
            android:name=".TopicsSelectImgsActivity"
            android:theme="@style/DarkyTheme.NoActionBar" />
        <activity android:name=".ChoosePlayerActivity" />
        <activity
            android:name=".TopicsSelectActivity"
            android:label="@string/title_activity_topics_select"
            android:theme="@style/DarkyTheme.NoActionBar" />
        <activity
            android:name=".LoginPageActivity"
            android:label="@string/sign_up" />
        <activity
            android:name=".MainActivity"
            android:theme="@style/AppTheme.Launcher">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>